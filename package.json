{
  "homepage": ".",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "bootstrap": "^4.5.3",
    "concurrently": "^5.3.0",
    "google-maps-react": "^2.0.6",
    "i": "^0.3.6",
    "newsapi": "^2.4.1",
    "react-bootstrap": "^1.4.0",
    "react-twitter-embed": "^3.0.3"
  },
  "scripts": {
    "start": "cd server && npm run dev",
    "heroku-postbuild": "npm cache clean && NPM_CONFIG_PRODUCTION=false npm install npm && run build"
  }
}
